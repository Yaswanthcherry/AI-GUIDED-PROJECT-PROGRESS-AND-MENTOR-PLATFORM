/**
 * Central UI branding — single source of truth for the product name.
 * Every visible reference to the product (page titles, logo lockup,
 * footer, landing/about copy) reads from these constants.
 */
export const PRODUCT_NAME = "AI-Guided Project Progress Tracking Platform";

/** Short brand mark used in the logo lockup. */
export const PRODUCT_WORDMARK = "AI-Guided";

/** Logo subline (mono, tracked uppercase). */
export const PRODUCT_SUBLINE = "PROJECT PROGRESS TRACKING";
