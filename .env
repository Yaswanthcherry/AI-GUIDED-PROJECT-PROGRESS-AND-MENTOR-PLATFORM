# AI-Guided Project Progress Tracking Platform — frontend environment.
# Backend base URL. Leave empty to run in mock mode (simulated agent layer,
# localStorage persistence — see src/services/api.ts for the endpoint contract).
# Point it at the FastAPI backend to go live, e.g.:
# VITE_API_BASE_URL=http://localhost:8000
VITE_API_BASE_URL=
